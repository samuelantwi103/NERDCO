name: mobile
description: "NERDCO Field App — National Emergency Response & Dispatch Coordination"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  web_socket_channel: ^2.4.0
  geolocator: ^12.0.0
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.0
  google_maps_flutter: ^2.6.0
  connectivity_plus: ^7.0.0
  shared_preferences: ^2.3.0
  # go_router: ^12.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: false
  uses-material-design: true
